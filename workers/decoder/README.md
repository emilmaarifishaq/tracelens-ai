# Decoder Worker

The decoder worker will own long-running PCAP processing jobs.

Initial implementation lives in `apps/api/app/services/decoder.py` for MVP simplicity. Move it here when asynchronous job processing is added.

