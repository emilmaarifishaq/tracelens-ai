{
  "name": "tracelens-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered trace analyzer for telecom and network protocol troubleshooting.",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace @tracelens-ai/web run dev",
    "build:web": "npm --workspace @tracelens-ai/web run build"
  }
}

